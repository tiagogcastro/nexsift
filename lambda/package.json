{
  "name": "@nexsift/lambda",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/publish dist/mcp && esbuild src/publish/handler.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/publish/index.js && esbuild src/mcp/handler.ts --bundle --platform=node --target=node22 --format=cjs --loader:.md=text --outfile=dist/mcp/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.1097.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.162",
    "@types/node": "^22.0.0",
    "esbuild": "0.28.1",
    "typescript": "6.0.3"
  }
}
